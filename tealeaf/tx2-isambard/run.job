#!/bin/bash

#PBS -q arm
#PBS -l select=1:ncpus=64
#PBS -l walltime=00:15:00
#PBS -joe
#PBS -N TeaLeaf

set -eu

cd "$RUN_DIR"

cp "$SRC_DIR/Benchmarks/tea_bm_5.in" tea.in

export OMP_PROC_BIND=true
export OMP_NUM_THREADS=2

aprun -n 64 -d 2 -j 2 -cc depth "./$BENCHMARK_EXE"
